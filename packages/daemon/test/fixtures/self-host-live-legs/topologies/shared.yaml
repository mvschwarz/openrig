# 51-09 live-leg fixture — LEG B COLLISION rig — deployed BYTE-IDENTICAL to BOTH hosts (the identity IS the collision under test; copy this one file, never author a second)
# Canonical seat = <pod>-<member>@<rigName> (deriveCanonicalSessionName) => lead-main@shared
version: "0.2"
name: shared
culture_file: culture.md
pods:
  - id: lead
    label: Lead
    members:
      - id: main
        agent_ref: "local:agents/lead"
        profile: default
        runtime: stub
        cwd: .
    edges: []
